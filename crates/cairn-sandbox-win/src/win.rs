// Placeholder — implementation lives in a later task.
// This file exists solely so rustfmt can resolve `mod win;` in main.rs
// without error; it carries no logic.

pub(crate) fn run() -> std::process::ExitCode {
    unimplemented!("Windows AppContainer launcher is not yet implemented")
}
